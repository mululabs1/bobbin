{
  "mcpServers": {
    "bobbin": {
      "command": "npx",
      "args": ["tsx", "${CURSOR_PLUGIN_ROOT}/packages/index-mcp-server/src/index.ts"],
      "cwd": "${CURSOR_PLUGIN_ROOT}",
      "env": {
        "BOBBIN_INDEX_DIR": ".bobbin/index"
      }
    }
  }
}
